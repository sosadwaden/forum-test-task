spring:

  application:
    name: forum

## Database Configuration
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
#    hibernate:
#      ddl-auto: #none disable hibernate schema generation
#  sql:
#    init:
#      mode: never #disable script-based schema generation

server:
  port: 7070

application:
  endpoint:
    topic: "/topic"
    message: "/message"

